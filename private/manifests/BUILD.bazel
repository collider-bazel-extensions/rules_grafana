package(default_visibility = ["//visibility:public"])

# The pinned, pre-rendered Grafana manifest. Generated by
# `bazel run //tools:render_writeback_<dotted_version>`.
exports_files(["grafana.yaml"])

filegroup(
    name = "manifest",
    srcs = ["grafana.yaml"],
)
